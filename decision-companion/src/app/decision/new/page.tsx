import DecisionWizard from '@/features/decision/DecisionWizard';

export default function NewDecisionPage() {
    return (
        <main className="min-h-[calc(100vh-4rem)] bg-gray-50 py-12">
            <DecisionWizard />
        </main>
    );
}
